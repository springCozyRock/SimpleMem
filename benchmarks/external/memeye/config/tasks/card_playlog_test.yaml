name: card_playlog_test

dataset:
  dialog_json: data/dialog/Card_Playlog_Test.json
  image_root: data/image

eval:
  mode: mcq
  output_json: output/results_card_playlog_test.json
  max_questions: 0
