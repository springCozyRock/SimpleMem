# GPU-specific dependencies (aligned with hsz server env: torch 2.5.0+cu121 on driver 535)
#
# Install BEFORE requirements.txt:
#   pip install -r requirements-gpu.txt
#   pip install -r requirements.txt
#
# Reinstall cu121 only (if requirements.txt accidentally upgraded torch):
#   pip install torch==2.5.0 triton==3.1.0 --index-url https://download.pytorch.org/whl/cu121 --force-reinstall

--index-url https://download.pytorch.org/whl/cu121
--extra-index-url https://pypi.org/simple

torch==2.5.0
triton==3.1.0
